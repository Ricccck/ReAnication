import * as React from "react";

import Box from "@mui/material/Box";

const Home = (props) => {
  const { user } = props;
  return (
    <Box className="Home">
    </Box>
  );
};

export default Home;
